// Small JS utilities (expandable)
document.addEventListener('DOMContentLoaded', ()=>{
  // Placeholder: add interactivity later (theme toggle, analytics, etc.)
  console.log('Personal card loaded');
});